<Javascript timeLimit="200" name="JS-ShredCredentialsJson">
  <!--
    Did not work on hybrid

    <Source>
    var c = JSON.parse(context.getVariable('private.credentialsjson'));
    for (var prop in c) {
      context.setVariable('private.' + prop, c[prop]);
    }
    </Source>
  -->
    <ResourceURL>jsc://shredCredentialsJson.js</ResourceURL>
</Javascript>
